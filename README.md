# Full-Stack-Todo-App-Using-Node.js-React.js-And-PostgresSQL

The app has two folders => client and server
Before running the project you should install node package in each folder
Run this command:
npm init -y
